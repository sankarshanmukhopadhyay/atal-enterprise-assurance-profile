# Ordering Model Example: Centralized Ordering

A single ordering service assigns sequence IDs for evidence objects before they are written.

Benefits: simple reconstruction; consistent ordering.  
Tradeoffs: ordering service is a critical dependency.
